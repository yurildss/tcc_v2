data:
  raw: data/raw
  processed:
    cut: data/processed/cut_beats
    augmented: data/processed/augmented
    split: data/processed/split
firmware:
  headers: firmware/generated_headers
pipeline:
  train_ratio: 0.7
  val_ratio: 0.15
  test_ratio: 0.15
